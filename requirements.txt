# --- Геоданные ---
geopandas>=0.14        # работа с геометрией, spatial join
shapely>=2.0           # геометрические операции
pyarrow>=15.0          # чтение/запись GeoParquet
pyproj>=3.6            # проекции координат

# --- Сбор данных ---
requests>=2.31         # HTTP: Overpass, OSRM, спутник
osmnx>=1.9             # удобная обёртка над OSM (опционально, для дорожного графа)

# --- Оценка нейросетью ---
openai>=1.30           # OpenAI-совместимый клиент (работает с Qwen через base_url)
pyyaml>=6.0            # чтение config.yaml
tenacity>=8.2          # ретраи вызовов API

# --- Оптимизация ---
pulp>=2.8              # MILP: P-Median / MCLP
numpy>=1.26
pandas>=2.2
scipy>=1.12            # корреляция Спирмена (валидация)

# --- Визуализация ---
folium>=0.16           # интерактивные карты рекомендаций
matplotlib>=3.8        # графики для отчёта

# --- Разработка ---
pytest>=8.0            # тесты
python-dotenv>=1.0     # загрузка .env с ключами
