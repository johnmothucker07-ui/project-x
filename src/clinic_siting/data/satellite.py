"""
ЭТАП 1 · Спутник — УЛУЧШЕНИЕ (в первой версии НЕ используется).
Заглушка заложена заранее, чтобы добавление фото не ломало архитектуру.

План: по координатам клетки скачать спутниковый тайл (Esri/Mapbox/Bing)
и передать его в тот же вызов VLM вместе с текстовой выжимкой.
"""
from __future__ import annotations


def fetch_tile(center_lon: float, center_lat: float, zoom: int, provider: str) -> bytes:
    """Скачать спутниковый тайл по координатам. Вернуть байты изображения."""
    raise NotImplementedError
